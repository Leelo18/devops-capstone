pipeline {
    agent any

    options {
        timestamps()
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }

        stage('Deploy (placeholder)') {
            steps {
                echo 'Deploying application (skipped for now)...'
            }
        }
    }
}
