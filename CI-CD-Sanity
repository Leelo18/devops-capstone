Jenkinsfile

pipeline {
  agent any

  options {
    timestamps()
  }

  stages {
    stage('Build') {
      steps {
        echo 'Building the project...'
      }
    }

    stage('Test') {
      steps {
        echo 'Running tests (placeholder)...'
      }
    }

    stage('Deploy (placeholder)') {
      steps {
        echo 'Deploy step will be updated later with Docker.'
      }
    }
  }

  post {
    success {
      echo 'CI-CD-Sanity pipeline completed successfully.'
    }
    failure {
      echo 'CI-CD-Sanity pipeline failed.'
    }
  }
}

Add Jenkinsfile for CI-CD-Sanity pipeline

